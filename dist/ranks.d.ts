/**
 * Leaderboard position markers.
 *
 * Medals stop at three, and the fallback used to be a bare number in a code
 * box, which read as a missing icon rather than a deliberate fourth place.
 * Numerals carry it to ten, which is as far as any of these lists go.
 */
export declare function rankIcon(index: number): string;
